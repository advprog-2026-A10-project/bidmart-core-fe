/**
 * OrderDTO — Output DTO returned by use-cases to the presentation layer.
 * Mirrors the domain entity shape without exposing branded types.
 */
export type OrderDTO = {
  id: string;
  // TODO: add output fields here
};
